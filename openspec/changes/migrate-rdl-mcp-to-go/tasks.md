# Tasks

## 1. Базовий контракт і підготовка

- [x] 1.1 Запустити наявні Python-тести й записати вихідний стан у `MIGRATION.md`; перевірка: команда `python3 -m pytest tests/ -v` та її результат зазначені у файлі.
- [x] 1.2 Зафіксувати fixtures для всіх 15 tools через Python stdio-сервер, включно зі схемами, default-ами, бізнес-відмовами й невалідними запитами; перевірка: baseline-запуск відтворює збережені JSON/XML результати.
- [x] 1.3 Узгодити міграційні правила в `AGENTS.md` і `CLAUDE.md`, створити Go module та мінімальний `cmd/rdl-mcp`; перевірка: `go build ./...` проходить і `MIGRATION.md` містить порядок зрізів.

## 2. Перенесення RDL

- [x] 2.1 Реалізувати читання/запис RDL 2016 через `encoding/xml` зі збереженням невідомого вмісту й просторів імен; перевірка: Go-тести порівнюють XML до/після точкової зміни на fixtures зі складними namespace.
- [x] 2.2 Перенести `describe_rdl_report`, `get_rdl_datasets`, `get_rdl_parameters`, `get_rdl_columns`; перевірка: Go результати структурно збігаються з Python baseline для порожніх, повних і фільтрованих даних.
- [x] 2.3 Перенести `validate_rdl` та розбір посилань на поля; перевірка: Go і Python однаково класифікують валідні, пошкоджені й неповні RDL fixtures.
- [x] 2.4 Перенести п'ять операцій із колонками Tablix; перевірка: диференційні тести порівнюють результати, XML після змін, межі індексів і автоматичну ширину сторінки.
- [x] 2.5 Перенести три операції з датасетами і дві з параметрами; перевірка: диференційні тести порівнюють успіх, дублікати, відсутні об'єкти та XML після змін.

## 3. MCP інтеграція

- [x] 3.1 Під'єднати зафіксовану версію офіційного MCP Go SDK і зареєструвати 15 tools з потрібними JSON Schema; перевірка: MCP SDK-клієнт отримує повний каталог із сумісними назвами й аргументами, а `go.mod` не має інших прямих залежностей без аппруву.
- [x] 3.2 Зв'язати tools з Go операціями, зберегти текстовий JSON result і default-и; перевірка: `tools/call` для всіх 15 fixtures збігається з Python за змістом результату.
- [x] 3.3 Реалізувати stdio-запуск, серіалізацію доступу до RDL і логування через наявні env-параметри; перевірка: інтеграційний тест перевіряє handshake, notification без відповіді, відновлення після помилок коректно сформованих викликів, завершення сеансу після невалідного JSON, EOF і чистий stdout.
- [x] 3.4 Провести спільний Go gate; перевірка: `go build ./...`, `go vet ./...`, `go test -race ./...` проходять, а всі прийняті розбіжності з Python описані в `MIGRATION.md`.

## 4. Перехід і дистрибуція

- [ ] 4.1 Зібрати Go-бінарники для підтримуваних платформ і перевірити локальний запуск у Claude Desktop та VS Code/Copilot через stdio; перевірка: обидва клієнти бачать 15 tools і викликають читання та редагування тестового RDL.
- [ ] 4.2 Підготувати MCPB із Go-бінарником, manifest і SHA-256, оновити `server.json`; перевірка: пакет запускається у підтримуваному клієнті, manifest і registry schema валідні. Якщо для пакування потрібна нова бібліотека чи залежність проєкту, спершу запросити окремий аппрув.
- [ ] 4.3 Оновити README, приклади конфігурації, версії та CI/release workflows для Go і позначити `uvx` як старий спосіб; перевірка: задокументована команда запускає Go-сервер, а CI збирає й тестує ті самі artifacts.
- [ ] 4.4 Після підтвердження паритету прибрати Python runtime/package та PyPI workflow; перевірка: збірка, тести, MCP smoke і документація не потребують Python, а повернення до попереднього PyPI release описане в `MIGRATION.md`.
- [ ] 4.5 Після окремого рішення про публікацію випустити Go artifacts і запис MCP Registry; перевірка: опубліковані файли, SHA-256, registry запис і установка з README відповідають одному тегу.
